## Agent skills

### Issue tracker / Issue 跟踪器

GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels / 分类标签

All five canonical labels use default names (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs / 领域文档

Single-context — `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
